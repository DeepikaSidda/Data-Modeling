# Screenshots

Place the screenshot images here, using these exact filenames so they render in the root `README.md`:

| File | What it should show |
|---|---|
| `01-workbench-aggregate-view.png` | NoSQL Workbench aggregate view (table + both GSIs) |
| `02-waitingroom-table.png` | WaitingRoom base table items and attributes |
| `03-waiting-index.png` | WaitingIndex (sparse GSI) — only WAITING entries |
| `04-eligibility-index.png` | EligibilityIndex (GSI) — ELIGIBLE / ACTIVE entries |
| `05-config-shard-count.png` | CONFIG item showing `Shard_Count = 4000` |
| `06-live-admit.png` | Live `POST /admit` API response |
| `07-live-status.png` | Live `GET /status` before vs after promotion |
| `08-load-test.png` | Load test output (exactly-once + shard distribution) |

PNG or JPG both work — if you use different names, update the image paths in the root `README.md`.
